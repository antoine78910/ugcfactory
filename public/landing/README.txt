Landing page assets (marketing only)

Workflow promo video (LP):
  Replace this file with your own MP4 (same filename):
    public/landing/workflow-promo.mp4

Do not use public/workflow-hero/ for the landing page — that folder is for the in-app workflow UI.

After replacing the video, hard-refresh the site or bump the cache query on the video src in src/app/page.tsx if a CDN still serves an old copy.
