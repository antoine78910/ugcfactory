/** Native spellcheck off for workflow prompts (brands, JSON, non-English copy). */
export const workflowDisableSpellcheck = {
  spellCheck: false,
  autoCorrect: "off",
  autoCapitalize: "off",
} as const;
