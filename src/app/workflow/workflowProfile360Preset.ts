/**
 * Fixed image-to-image instruction for the 360° profile preset (no user-written prompt).
 */
export const PROFILE_360_IMAGE_PROMPT = `Using only the attached product reference photo, output a single ultra-realistic e-commerce packshot of the same product on a seamless light-gray studio sweep. Use a clean three-quarter angle as if the product were mid-rotation on a 360° turntable: centered, sharp edges, labels and logo fully legible, soft diffused studio lighting, subtle contact shadow, no extra props, no people, no text overlays, no collage.`;
